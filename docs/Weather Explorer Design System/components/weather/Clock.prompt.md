Compact accessible live local-time clock for the header (FR-CLOCK-01). Updates every second in tabular mono so the width stays steady.

```jsx
<Clock />
<Clock timeZone="Europe/Kyiv" withDate />
```

Pass a `timeZone` to show a city's local time. `role="timer"` with an accessible label.
