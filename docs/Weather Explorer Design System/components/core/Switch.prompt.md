Calm pill toggle for binary state — theme day/night, the "Compare weekend" view switch.

```jsx
<Switch checked={dark} onChange={setDark} label="Нічна тема" />
```

Knob slides on a sky track. Pass `label` to render a clickable text label.
